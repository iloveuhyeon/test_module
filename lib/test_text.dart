import 'package:flutter/material.dart';

class PrintText extends StatelessWidget {
  const PrintText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("TestModule");
  }
}
